{% extends "base.html.twig" %}

{% block title %}Challenge Details{% endblock %}

{% block content %}
    <div class="challenge-view">
        <p>Started At: {{ challenge.started_at|date('Y-m-d H:i') }}</p>

        {% if isCompleted %}
            <p>✅ Completed At: {{ challenge.completed_at|date('Y-m-d H:i') }}</p>
            <p class="success-message">You have completed all the exercises! Great job, Gondorian!</p>
        {% else %}
            <p>❌ Not completed</p>
            <p class="encouragement-message">You gave it a valiant try. Perhaps you wish to try again today?</p>
        {% endif %}

        <div class="link-group">
            <a href="/quests" class="quest-link">Back</a>
            <a href="/daily-quest/start" class="quest-link">Try Again</a>
        </div>
    </div>
{% endblock %}
